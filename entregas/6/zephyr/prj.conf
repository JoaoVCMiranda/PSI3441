# Console via UART0
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_SERIAL=y
CONFIG_PRINTK=y

# GPIO para os LEDs
CONFIG_GPIO=y

# Threads: habilitadas por padrão no Zephyr; sem config extra necessária
# CONFIG_NUM_PREEMPT_PRIORITIES default=15 (suficiente para prioridades 5 e 6)
