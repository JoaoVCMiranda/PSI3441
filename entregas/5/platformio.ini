; IDE + IntelliSense — build real: make
; Entrega 5: bare-metal + pwm_z42 (Prof. Rehder)

[platformio]
description = Entrega 5 — HC-SR04 + PWM radar

[env:frdm_kl25z]
platform = freescalekinetis
board = frdm_kl25z
src_dir = .
src_filter = +<main.c> +<pwm_z42.c>
build_flags = -I../include
